experiment_id: go-term-distance
config_version: 0
owner: Ethan
claim: >
  Disagreement among models (or vs trusted annotations) can be quantified with
  GO / functional-category graph distance, not only exact string match.
status: planned

models:
  extractors: []
  consensus: null

fixtures:
  genes: fixtures/gene_sets/PLACEHOLDER.json
  gold: fixtures/gold/PLACEHOLDER.json

conditions:
  - extractors_k3_plus_consensus
  - baselines_as_needed

metrics:
  - go_graph_distance
  - exact_match_rate
  - partial_overlap_rate

notes: |
  Coordinate metric definition with Braden; reuse compareannotations / goresolve where possible.
